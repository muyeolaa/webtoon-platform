플렛폼별로 타이틀id의 형태가 다를수도있고 중복될수도있는데 이경우는 어떻게 해결하는지
모듈파일에 인수? 어떤게있는지
nest내장 메소드? 같은거 다 찾아보기
바닐라 js 능력좀 올리기 map 반복문 if문 등등
제네릭 함수 복습

========================================
nest 데코레이터 종류 알아보기

@Injectable
@InjectRepository

============================
바닐라 js 지식 쌓기

js 메소드? 종류

data.indexOf ( )
data.substring ( )

=================================
프로젝트 진행하면서 수정 & 추가할거

반응형 적응형 테스트

동적 쿼리?

// 🚀 1. 타입을 Promise로 감싸줍니다 (Next.js 최신 문법!)

완성되면 회차를 클릭할때 회차에맞게 보내는 방법 고민하기

검색 할때 요일필터가 계속 적용이되어있어 검색에 문제가 있음

DB에서 인덱스가 무슨 기능을하는지

일단 순서는 카카오 웹툰 회차 수집을하고 회차정보를 받은다음 모달창의 UI를 완전 바꿀거야 크기부터 배치까지 그리고 아까말했던 사이트에서 회차를 누르면 플렛폼에 맞는 웹툰 회차로 보내는 방식

❓ "TypeORM에서 DB 외래키 컬럼명(webtoon_db_id)과 코드의 객체 변수명(webtoon)을 다르게 설정하는 이유 복습하기 (feat. @JoinColumn의 역할)"

[실무/개념 중심 메모]

📚 "객체 지향 프로그래밍(TypeScript)과 관계형 데이터베이스(SQL)의 패러다임 불일치(Impedance Mismatch) 개념 이해하기"

[세부 복습 리스트]

DB는 왜 객체가 아니라 '번호표(외래키, FK)'만 저장해야 하는가? (데이터 중복 방지)

에피소드 테이블에 문자열 복합키(naver_769209_1) 대신 단순 숫자(id)를 Primary Key로 쓰는 이유 (인덱싱과 성능)

TypeORM이 DB의 번호표를 읽어서 어떻게 객체로 번역해 주는지(relations)

이렇게 적어두면 나중에 나한테 "예전에 적어둔 '객체와 RDBMS의 패러다임 불일치'에 대해서 다시 설명해 줘!" 라고만 해도 내가 오늘 상황과 연관 지어서 기가 막히게 다시 설명해 줄 수 있어. 😉

메모장에 잘 적어뒀어? 그럼 이제 아까 말했던 '부모 웹툰 찾아서 연결하는 크롤러 수정 코드'를 적용해서, 에피소드 테이블에 null 대신 진짜 데이터가 쏙쏙 들어가는 걸 눈으로 직접 확인해 볼까?

- 'SSR(서버 렌더링) 하이드레이션 타이밍 이슈'와 '리액트 앱 크래시(React Crash)'가 동시에 터진 상황이야.

- 의존성 주입? 그게뭔데
